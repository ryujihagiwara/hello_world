<div class="header">
  <div class="header__left">
    <p>logo</p>
  </div>

  <div class="header__right">
    <% if learner_signed_in? %>
  	  <%= link_to 'My Page', learner_path(current_learner) %>
  	  <%= link_to 'Moments', posts_path %>
  	  <%= link_to 'Learners', learners_path %>
  	  <%= link_to 'Sign Out', destroy_learner_session_path, method: :delete %>
  	<% else %>
      <%= link_to 'About', homes_about_path %>
      <%= link_to 'Moments', posts_path %>
      <%= link_to 'Sign Up', new_learner_registration_path %>
      <%= link_to 'Sign_in', new_learner_session_path %>
    <% end %>
  </div>
</div>
